#definition
### Types
- `w` : [[Word]]
### Definition
`w` is said to be *reduced* when non of its elements are adjacent to their inverses. Moreover, if`a` and `b` and `ab` are elements in `S`, then when `a` comes before `b` in `w`, both `a` and `b` should be replaced by `ab`.